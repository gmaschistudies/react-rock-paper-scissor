import React, { FunctionComponent as FC, ReactElement as RE } from 'react';
import { StyledLastPlayCard } from './LastPlayCard.styles';

const LastPlayCard: FC = (): RE => {
  return <StyledLastPlayCard />;
};

export default LastPlayCard;
