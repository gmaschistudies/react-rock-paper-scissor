import React, { FunctionComponent as FC, ReactElement as RE } from 'react';
import { StyledScoreCard } from './index.styles';

const ScoreCard: FC = (): RE => {
  return <StyledScoreCard />;
};

export default ScoreCard;
